// Shim — the Cambridge results page now lives in features/cambridge/surfaces/results/.
// Kept so existing imports (App.js lazy route) keep working unchanged.
export { default } from '../features/cambridge/surfaces/results/CambridgeTestResults';
