// Shim — the legacy IELTS landing now lives in features/landing/surfaces/v1/.
// Kept so existing imports (App.js route /landing/v1) keep working unchanged.
export { default } from '../features/landing/surfaces/v1/LandingV1Page';
