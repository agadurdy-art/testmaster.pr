// Shim — the Full Test interface now lives in features/fulltest.
// Kept so existing imports (App.js lazy route) keep working unchanged.
export { default } from '../features/fulltest/FullTestInterfacePage';
