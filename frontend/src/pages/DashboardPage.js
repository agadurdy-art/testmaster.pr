// Shim — the IELTS D1 dashboard home now lives in features/dashboard/surfaces/main/.
// Kept so existing imports (App.js lazy route /dashboard and /dashboard/v2) keep working unchanged.
export { default } from '../features/dashboard/surfaces/main/DashboardPage';
