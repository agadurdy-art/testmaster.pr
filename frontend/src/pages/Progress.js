// Shim — the Progress page now lives in features/progress/.
// Kept so existing imports (App.js lazy route /progress) keep working unchanged.
export { default } from '../features/progress/ProgressPage';
