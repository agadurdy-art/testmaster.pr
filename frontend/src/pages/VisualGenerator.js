// Shim — the admin Task 1 visual generation tool now lives in features/admin/visual_generator/.
// Kept so existing imports (App.js AUTH-gated route /admin/visual-generator) keep working unchanged.
export { default } from '../features/admin/visual_generator/VisualGeneratorPage';
