// Shim — the shared profile page now lives in features/profile/.
// Kept so existing imports (App.js route /profile) keep working unchanged.
export { default } from '../features/profile/ProfilePage';
