// Shim — the mode-branched legacy dashboard now lives in features/dashboard/surfaces/legacy/.
// Kept so existing imports (App.js route /dashboard/legacy) keep working unchanged.
export { default } from '../features/dashboard/surfaces/legacy/LegacyDashboardPage';
