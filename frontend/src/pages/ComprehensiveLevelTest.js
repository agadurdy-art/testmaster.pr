// Shim — the comprehensive placement test now lives in features/placement/comprehensive/.
// Kept so existing imports (App.js lazy routes /comprehensive-level-test and
// /ge/placement-test with mode="ge") keep working unchanged.
export { default } from '../features/placement/comprehensive/ComprehensiveLevelTestPage';
