// Shim — the Question Bank page now lives in features/questionbank/.
// Kept so existing imports (App.js lazy route) keep working unchanged.
export { default } from '../features/questionbank/QuestionBankPage';
